<!-- Your code here -->
<h1>Hello World!</h1>
<!-- Save params[:name] to variable @namess-->
<!-- <h2>Hello <%= @name%>!</h2> -->

<%= form_with local: true, method: "get" do |form| %>
  <p> City: <%= form.text_field :city %>
	<%= form.submit %>
<% end %>

<% City.all.each do |c| %>
  <% if not c.nil? %>
    <h2> <%= c.name %></h2>
    <p>It's <%= c.temperature %> °F with <%= c.description%>.</p>
  <% end %>
<% end %>
